# Zuri Market Frontend

This is the React frontend for the Zuri Market project.

## Setup

Install dependencies:

```bash
npm install
```

Run locally:

```bash
npm start
```

> The frontend expects the backend to be available at `http://localhost:5000` during development.

Build for production:

```bash
npm run build
```
